"""Reusable event timeline component for FlowDesk.

Renders a chronological event timeline for a given ticket's lifecycle
events.
"""

import streamlit as st


def render_event_timeline(events: list[dict]) -> None:
    """Render the event timeline for a ticket.

    Args:
        events: A list of event dictionaries ordered chronologically.
    """
    pass
